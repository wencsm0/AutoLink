server:
  port: 8081

spring:
  datasource:
    url: "jdbc:sqlite:resources/auto_link.db"
    driver-class-name: org.sqlite.JDBC
  jpa:
    show-sql: true
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
